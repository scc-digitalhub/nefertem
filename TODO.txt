Store

Store in readonly
Store metadata per output (metadata + artifact) (loacle di default)
NTH - Registro store, non store handler
passare a importlib

Plugin

passare a importlib
spostare type constraint in base


Versioning

Dichiarare versione supportata in plugins, operazioni e core. Plugin -> operazioni -> core


Run

iniettare validate, infer, profile in run handler, da function definita in operazione base
